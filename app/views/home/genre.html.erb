

<table class="bordered-table sort zebra-striped">
  <thead>
    <tr>
      <th>Žanri</th>
      <th>Št. naslovov</th>
			<th>Povprečno št. naslovov na št. žanr</th>
    </tr>
  </thead>
  <tbody>
		<% @genres.each do |genre| %>
			<tr>
        <td><%= genre.genre  %></td>
        <td><%= genre.books.count %></td>
				<td><%= @avrage %></td>
      </tr>
		<% end %>
  </tbody>
</table>